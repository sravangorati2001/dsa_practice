bool isSubsequence(string s, string t) {
        int ls=s.size(),lt=t.size(),j=0;
        for(int i=0;j<ls and i<lt;i++)
            if(t[i]==s[j])
                j++;
        if(j==ls) return true;
        return false;
    }
